# Mistral-OCR
Full stack app using MistralOCR API for document in form of image (scan) or PDF conversion to Markdown and JSON, including extraction of images from document for independent processing 
